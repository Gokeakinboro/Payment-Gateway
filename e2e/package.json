{
  "name": "paylode-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Paylode persona smoke (Playwright) — log in as each role, assert the correct landing page.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
