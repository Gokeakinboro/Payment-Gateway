---
name: feedback-paylode-money-signoff
description: "User wants Paylode money/settlement items staged for sign-off before deploy, after all other fixes + mail investigation"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 5042ea10-d387-4f4d-ace7-9c60ba872118
---

For the Paylode 2026-06-14 fix list ([[outstanding-tasks]]), the user (2026-06-14) directed: **do NOT auto-deploy the money items #1/#10/#11** (merchant credit wallet + SA top-up + prepaid payout math; card/VA payer-funded settlement math). Instead **STAGE them for the user's sign-off**, and only after (a) all the other fixes are done AND (b) the "New onboarding.jpg" email investigation is complete.

**Why:** money split/settlement logic is high-stakes on a live gateway; user wants to review before it goes live.

**How to apply:** Build #1/#10/#11 on a separate branch or leave committed-but-undeployed (clearly marked); present the logic for review; deploy only after explicit approval. Non-money fixes (#3 aggregator self-onboarding, #5 settlement-account change approval) can deploy as normal. Confirmed funding rules already captured in [[outstanding-tasks]] (txn amount lands with merchant; payer covers our charge + VAT for cards/VA; payouts prepaid = txn + rail fee + VAT from merchant credit).

**⚠️ LIVE BUG FOUND + FIXED BY STAGED BRANCH (2026-06-14):** deployed checkout.js ALREADY calls `computeFeesForTxn` (5 places) but deployed feeEngine.js never exported it → live CARD CHARGE path throws "computeFeesForTxn is not a function" (currently broken; masked because Interswitch still sandbox/not-live). The staged branch's feeEngine.js ADDS computeFeesForTxn → fixes the break AND implements payer-funded. So deploying feeEngine.js is a net positive. (computeFeesWithConfig used by transactions.js lives in helpers.js, not feeEngine — fine.)

**PAYOUT REMINDER (user 2026-06-14, msg cut off):** for payouts, the merchant account MUST be funded before any transaction goes through, and every single transaction is netted off the [wallet balance — sentence truncated, confirm rest]. Current payouts.js already enforces funded-wallet (rejects INSUFFICIENT_BALANCE with amount+fee+VAT breakdown) and deducts per batch (items itemized). User emphasis: fund-before + per-transaction netting. Confirm the truncated tail.

**PAYOUT PER-RAIL DESIGN — FINAL (user 2026-06-15), building on branch feature/payout-per-rail-wallets (review before deploy):**
- Applies to **PAYOUTS ONLY** (cards/VA inbound untouched).
- **Rails are INTERNAL — never shown to merchants anywhere (hard NO).** Merchant sees ONE total balance only. Paylode chooses rails to spread payout traffic + steer away from problem rails; rails can change.
- Wallet is **per (merchant, rail)** — MerchantWallet gets railId; unique (merchant_id, rail_id); WalletLedger gets railId; PaymentRail gets `payoutEnabled` flag. (Greenfield: 0 existing balances, all rails CONFIG_ONLY, so no migration of balances needed.)
- **Every SA credit MUST be matched to a rail.** SA can credit AND debit a (merchant,rail) balance, pick/override the rail. **Balance = spend cap** (deplete to 0; SA tops up to allocate more volume to that rail).
- **Routing = HYBRID:** payout that fits one rail (spread by lastUsedAt / priority) auto-sends; payout exceeding any single rail OR short on the chosen rail → goes to an **SA routing queue** (batch status e.g. needs_routing) for manual rail assignment; reject only if TOTAL across all the merchant's rails < payout total.
- **LATENT BUG to fix:** routeTransaction(...,'PAYOUT',...) queries rail_costs.service_type which DOESN'T EXIST (col is `channel`, enum has no PAYOUT) → always throws (silently caught) → payouts never got a rail. New per-rail model + payoutEnabled replaces this for payout routing.
- **KIV (future enhancements):** (a) tracking/inputting the RAIL'S OWN balance (bank-account liquidity at the rail) — offline/SA knowledge for now; later manual input or auto via rail API. (b) splitting a SINGLE payout across multiple rails automatically (v1 = single rail auto, else SA queue). (c) **RAIL FAILURE HANDLING (user 2026-06-15 SPEC):** When a payout rail is failing (from error messages returned by the rail's API), (1) INFORM SA + the failure REASON (email). (2) For each rail we integrate, check if the rail/bank API lets us query OUR account BALANCE with them; if it does, and txns are failing due to LOW BALANCE, email SA to take action (e.g. reroute that merchant's payout traffic through another rail). Build these checks INTO each rail adapter as we integrate it. Foundation to build: a reusable SA-alert (notifyRailIncident: rail+reason+suggested action, debounced), per-rail health tracking (consecutive failures threshold), and a rail-adapter contract (sendPayout(), optional getBalance()). Live detection wires up when the first real payout rail is integrated (collaborative). Ties to the per-rail wallet routing already built (SA can steer traffic by funding a different rail / the routing queue).
- **DEPLOYED 2026-06-15:** per-rail payout build merged to main + migrated to prod (greenfield, 0 balances). SA must enable a rail (payoutEnabled + LIVE) before crediting merchants. (d) ✅ **LIVE PAYOUT — DONE (user 2026-06-20: PalmPay is live, real disbursement wired & working; "live payout test is stale, take it off the list").**

**MONEY FLOW STATUS SUMMARY (2026-06-15):** ACCOUNTING/LOGIC layer is DONE+deployed end-to-end — inbound payer-funded charge math (#11), settlement = full principal to merchant, prepaid payout wallet (#10) + per-rail allocation/routing. REMAINING (not logic, but EXECUTION + finishing): (1) Interswitch still SANDBOX, not production — real card money not moving yet (go-live blocker, see [[project-paylode]]/[[outstanding-tasks]]). (2) ✅ DONE (user 2026-06-20) — actual payout disbursement to beneficiary banks via PalmPay is wired & working (live). (3) VAT netting (output−input) on the card path — KIV (pay-in netting + VAT report already built). (4) Rail-failure handling — KIV (together). (5) Live payout test — DONE/stale, removed (user 2026-06-20). **STILL OPEN:** PalmPay payout WEBHOOK reconciliation — confirm it matches legs by `rail_order_id` and rolls up the final settle status (disburse currently marks success synchronously on the create response; the webhook is the authoritative settle confirmation).

**MONEY CODE AUDIT (2026-06-14, read-only — nothing deployed):**
- **#1 SA fund merchant wallet — ALREADY FULLY BUILT.** Backend: `POST /payouts/wallet/fund` (requireSuperAdmin) + `merchant_wallets` + `wallet_ledger` + `GET /payouts/wallet`, `/wallet/ledger`, `/payouts/admin/wallets`. Frontend: `loadWallets()` page with "Fund Wallet" button → `fundWallet()`, ledger view (api-wiring.js ~3813). **ONLY GAP:** there is NO `wallets` item in NAV.superadmin/admin, so the page is unreachable → that's why user "didn't see where SA adds credits". FIX = add a `wallets` nav item (trivial; just exposes existing UI). renderPage falls back to overview then loadPageData('wallets')→loadWallets() overwrites, so it works once nav entry exists.
- **#10 prepaid payout math — ALREADY BUILT.** payouts.js POST /batches: per item `total = amount + fee + vat`; `totalDeduction = totalAmount + totalFee + totalVat` checked against + drawn from wallet; insufficient-balance error breaks down amount+fee+VAT. OPEN QUESTION for user: the `fee` is the PAYOUT rate from merchantRateConfig/platformRateConfig (channel PAYOUT/ALL) = Paylode's payout fee. User's #10 said "rail fee + VAT" — confirm whether merchant should be charged the RAIL COST pass-through or Paylode's payout fee (currently the latter).
- **VAT HANDLING — KIV (user 2026-06-14, rule given; implement LATER once money flow concluded).** RULE: the VAT the RAIL charges us on their fee (input VAT) is NETTED OFF the VAT we charge the payer/merchant (output VAT); the DIFFERENCE is what we remit to the tax authority. i.e. **VAT payable = output VAT (vatOnFee, on our fee) − input VAT (vatOnRail, charged by rail)**. Also REQUIRED: a **VAT report** (output VAT collected, input VAT paid, net VAT payable, by period). 
  - Staged branch's computeFeesForTxn currently computes vatOnFee but treats the WHOLE vatOnFee as remitted and does NOT yet net off vatOnRail — UPDATE when implementing: net remit = vatOnFee − vatOnRail; surface both for the VAT report. DO NOT deploy until the whole money flow is concluded + signed off.
- **#11 card/VA payer-funded — NEEDS A CHANGE (the real money work).** Currently transactions.js stores txn.amount (full), merchantFee, railCost separately; feeEngine.computeFees gives merchantFeeWithVat, railCostWithVat, netRevenue = merchantFee−railCost, aggShare, paylodeMargin. Current model appears to DEDUCT fee from txn (merchant absorbs → settlement = amount − fee), the traditional model. User's #11 wants PAYER-FUNDED: payer pays `amount + our charge (+VAT)`, merchant settled the FULL `amount`, rail gets `amount + our charge` then takes its cut, Paylode keeps `our charge − rail cost`, VAT remitted. CHANGE NEEDED: checkout charges payer amount+charge+VAT; settlement = full amount (not amount−fee); expose "amount_charged_to_payer" in txn/checkout response. This is the high-stakes change — DO NOT deploy without sign-off.
