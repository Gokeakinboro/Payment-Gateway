---
name: project-paylode-dev-deploy
description: Paylode dev/deploy workflow + STALE local-checkout warning; how backend/frontend ship to prod
metadata: 
  node_type: memory
  type: project
  originSessionId: 018c26fc-7a85-4657-9d96-209516c7b73e
---

Paylode dev + deploy workflow (verified 2026-06-23). Relates to [[project-paylode]], [[project-paylode-kyc-admin-docs]].

**🟢 2026-07-09 — PRIVACY POLICY LIVE** at https://paylodeservices.com/privacy.html (PR #107): self-contained NDPR-aligned `privacy.html` + homepage footer link + added to the deploy manifest. **⚠️ BUT the GH Action frontend deploy SILENTLY NO-OP'd on 45** — run concluded "success" yet 45's index.html stayed stale (09:08) and privacy.html never landed (the appleboy/scp-action step didn't error the job). Had to **scp privacy.html + index.html DIRECTLY to 45 + 176 `/var/www/paylode/`** to publish (origin then served 200, Cloudflare 200). Lesson: after a push-to-main frontend deploy, VERIFY the file actually landed on 45 (`ssh root@45.141.122.223 ls -la /var/www/paylode/<file>`) — don't trust the green Action; scp directly if missing. Reliable manual publish = `scp <files> root@45.141.122.223:/var/www/paylode/` (+176). Live web origin = 45 behind Cloudflare (Cloudflare may cache; bust with `?x=<ts>`).

**⚠️ CORRECTION 2026-07-09 (verified live on 176):** the backend pm2 app is **`paylode-core`** (cluster, 2 instances id 15+19), on port **3001** — NOT `paylode-api:3000` as CLAUDE.md + older notes below say. Reload = **`pm2 reload paylode-core`**. There's no `/health` route (returns 404); use a real route to smoke, e.g. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3001/api/v1/checkout/<ref>` → 200. Other pm2 apps on 176: paylode-webhook-worker (delivers queued webhooks), paylode-invoicing(+worker), paylode-wallet, paylode-assistant. Fast per-file deploy that WORKS: `scp <file> root@176.57.188.45:/opt/paylode-api/backend/<path>` → ssh `sed -i 's/\r$//' <file>` (strip Windows CRLF) → `node -c <file>` → `pm2 reload paylode-core`. Verify box==main base first with `md5sum` (git show main:file | tr -d '\r' vs remote) to avoid clobbering prod drift.

**NO LOCAL CHECKOUT.** The stale `Desktop\Paylode\paylode-full` clone (was ~90 commits behind prod) was DELETED 2026-06-23 along with its worktree. **Repo of record = `origin/main`** (github `Gokeakinboro/Payment-Gateway`). To work on Paylode again, clone fresh: `git clone https://github.com/Gokeakinboro/Payment-Gateway.git`. Never resurrect/trust the old folder. Lesson: I rebuilt an already-shipped sandbox feature off the stale tree before catching the drift — always check `git log origin/main` first. Deploy topology + commands are now committed in-repo at `docs/DEPLOYMENT.md`.

**TOPOLOGY (post-migration):** 176.57.188.45 = backend/DB/processing (paylode-api:3000, api.paylodeservices.com). **45.141.122.223 = the LIVE web server** — static `/var/www/paylode` for paylodeservices.com is served from 45, with /api proxied to 176. So a **backend change ships to 176**. Same root SSH password works on BOTH boxes (user-held; not stored here). **45 has NO git checkout** — it's a static `/var/www/paylode` only (no `/opt/paylode-api`).

**✅ FRONTEND AUTO-DEPLOY FIXED 2026-06-25 (the GH Action now reaches users).** Previously the Action only updated 176 (`SERVER_HOST`) via on-box `git checkout`, so frontend pushes NEVER reached the live 45 box — you had to manually scp to 45. Now `.github/workflows/deploy.yml` deploys frontend to **BOTH**: (1) **45 (live)** — `appleboy/scp-action` pushes the 8 frontend files from the runner to `/var/www/paylode` (no git needed) + nginx reload; (2) **176 (fallback)** — the original git-checkout deploy, `if: always()` so failover never goes stale. Added secret **`WEB_HOST=45.141.122.223`**. The Action's key (`SERVER_SSH_KEY`) only authorized 176, so I generated a **dedicated ed25519 deploy key `github-actions-deploy-paylode`** (fingerprint `SHA256:a7lFH4caTnlQZrPo6BcbqVJz5n/02Dd+5WpVe13qGSY`), appended its pubkey to root@45 AND root@176 `authorized_keys`, set its private key as `SERVER_SSH_KEY`. Verified: a normal push to `main` → all steps green → live site updated. **So a plain push to `main` now reaches users; the manual `deploy.py --frontend` to 45 is only a fallback.** Cache-bust (`?v=NN`) still needed for app.js/api-wiring.js changes.

**✅ LANDING PAGE REWRITE shipped 2026-06-25 (commits 1eb336c→94c22d5):** `index.html` rebuilt from a portal-login page into a product-led marketing page — leads with Collections (cards/virtual accounts/transfers) + Payouts, a "Why Paylode" benefits grid, 3-step Get Started, CTA band. The 4 portal login cards were replaced by a single **Log In icon → modal** (pick account type → credentials; ports login.html's full flow incl. 2FA + forgot-password). **Create Account** button (label kept) opens a Merchant/Aggregator chooser dropdown. Same navy/green/DM-Sans design system.

**Deploy** = `tools/deploy.py` (paramiko SFTP, root). Runs `tools/check-syntax.mjs` gate + git-clean gate (commit first or `--allow-dirty`) → backs up to /root/deploy-backup-* → uploads → md5-verifies. Needs `PAYLODE_SSH_PASS` env (USER-HELD).
- Backend (default 176): `PAYLODE_SSH_PASS=… python tools/deploy.py` (whole manifest). Then `pm2 reload paylode-api --update-env` over ssh + `curl localhost:3000/health`=200 (deploy.py does NOT restart).
- Frontend to 45: `PAYLODE_HOST=45.141.122.223 PAYLODE_SSH_PASS=… python tools/deploy.py --frontend` (filters to /var/www/* targets). No restart needed (nginx serves static). python3.14+paramiko available locally; run with sandbox disabled (needs egress to :22).

**Frontend cache-bust:** bump `api-wiring.js?v=NN` (and `app.js?v=NN`) in `dashboard.html`. Pushing to `main` triggers the GH Action = FRONTEND ONLY, now to BOTH 45 (live, scp) + 176 (fallback, git checkout) — see the AUTO-DEPLOY FIXED note above. It does NOT pull backend src (backend ships via deploy.py). So a backend change must go through deploy.py, not the push.

**Shipped 2026-06-23 (commit 64d240e on main):** `POST /api/v1/merchants/:id/resend-sandbox` (SA/admin/compliance) — ensures sk_test/pk_test keys (mints missing; `rotate:true` forces fresh), optional temp-password reset (default on), emails sandbox creds; only freshly-issued keys revealed. Frontend "Resend Sandbox" button on merchant detail modal (api-wiring v83). NOTE: prod already had sandbox-on-signup + auto-flip-to-live (`provisionMerchant` in onboarding.js, sandbox keys work pre-KYC in middleware/auth.js) — don't rebuild.
